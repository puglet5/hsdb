<% if obj.errors.full_messages.any? %>
  <div class="absolute self-end right-2 top-14">
    <% obj.errors.full_messages.each do |msg| %>
      <%= render(FlashComponent.new(msg: msg, type: 'error', cls: "flash-error")) %>
    <% end %>
  </div>
<% end %>
