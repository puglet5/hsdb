<div class="container">
  <p id="notice"><%= notice %></p>
  <h1>Uploads</h1>
  <%= link_to 'New Upload', new_upload_path, class: "btn", style: "background-color: #1E51A4 !important;"%>
  <%= render "search_form"%>
  <div class="row">
    <%= render @uploads %>
  </div>
  <div>
    <%= paginate @uploads %>
  </div>
</div>
