<%= render Sections::SectionComponent.new(cls: "text-gray-700") do %>
  <%= render Sections::DivContainerComponent.new() do %>
    <div class="flex flex-col">
      <h1 class="mb-2 text-4xl font-bold tracking-tight leading-none text-center text-gray-900">Forum</h1>
    </div>
    <% if @discussions_pinned.count > 0 %>

      <%= render "shared/pinned_discussions" %>
    <% end %>
    <%= render "shared/latest_discussions" %>

  </div>
<% end %>
<% end %>
