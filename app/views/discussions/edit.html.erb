<section class="bg-gray-50 dark:bg-gray-900 border pb-10">
  <%= render "shared/errors", obj: @discussion %>
  <div class="container mx-auto p-20">
    <h1 class="flex justify-center text-4xl mb-6 font-semibold">Edit Discussion</h1>
    <%= render "form", discussion: @discussion %>
  </div>
</section>
